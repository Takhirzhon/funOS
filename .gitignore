# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
TODO.md

# Deploy state, written on the server by deploy/auto_deploy.sh. Both must stay
# untracked: `git reset --hard` leaves untracked files alone, which is the only
# reason the deployer can remember anything across ticks.
.deployed_sha
.ci_wait
.refused
deploy/.env
